string catAndMouse(int x, int y, int z) {
    int a=abs(x-z);
    int b=abs(y-z);
       if(a==b){
           return "Mouse C";
            
       }
    else if(a>b){
        return "Cat B";
    }
    else {
        return "Cat A";
    }

}
