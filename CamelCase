int count = 1;
 
    for (int i = 1;s[i]!='\0' ; i++) {
        if (isupper(s[i]))
            count++;
    }
 
    return count;
    // int count=0;
    // for(int i=0;s[i]!='\0';i++){
    //   if(s[i]>=65 || s[i]<=90){
    //       count++;
    //   }
    // }
    // return count+1;
